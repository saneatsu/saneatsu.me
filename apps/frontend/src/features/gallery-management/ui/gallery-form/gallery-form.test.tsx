import { describe, it } from "vitest";

describe("GalleryForm", () => {
	// TODO: GalleryFormのテストを追加する
	// 現在は外部依存（Mapbox, ImageUploader, RadixUI）が多く、
	// テストワーカーがクラッシュするため、モック戦略の見直しが必要
	it.todo("更新成功後にリダイレクトせず現在のページに留まる");
});
