# Lefthook configuration
# https://lefthook.dev/configuration/

# pre-commit フック設定
pre-commit:
  parallel: false  # テストが順番に実行されるようにする
  commands:
    # Biomeでコードフォーマットとlintを実行
    biome:
      run: pnpm exec biome check --write --no-errors-on-unmatched {staged_files}
      glob: "*.{js,jsx,ts,tsx,mjs,cjs,json,md,yml,yaml}"

    # TypeScriptの型チェック
    type-check:
      run: pnpm type-check
      # 型チェックは全体を対象にするため、staged_filesは使わない

    # ルートのvitest workspace設定でapps/backend, apps/frontendのテストを一括実行
    vitest:
      run: pnpm vitest

    # apps/frontendのStorybook テストを実行
    storybook-test:
      run: cd apps/frontend && pnpm test-storybook:smart
      # test-storybook:smartは既存サーバーがあれば使用、なければCI modeで起動

# pre-push フック設定
pre-push:
  commands:
    # Turboキャッシュを無視してTypeScriptの型チェックを実行
    # --force を使う理由: next-intl.d.ts などの型拡張ファイル変更時に
    # Turboキャッシュが原因で型エラーが検知されないケースがあったため、
    # push前には必ずフルチェックを行う
    type-check:
      run: pnpm turbo run type-check --force

# commit-msg フック設定
commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
